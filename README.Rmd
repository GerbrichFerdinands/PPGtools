# PPGtools

## Example

```{r}
# prepare data
data <- prepInput(rec, channel = "Green", tstart = 20, tstop = 40)

# smooth series z 
z <- smoothWE(raw_signal = data, lambda = c(0, 10, 100))

# plot result
plotLambda(data, z, "test")

```


## Package structure
![](flowchart.png)
